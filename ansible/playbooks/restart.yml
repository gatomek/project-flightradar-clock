- name: Restart radar-clock on castle
  vars_files:
    - vars/prod.yml
  gather_facts: no
  hosts: castle
  become: true
  become_method: sudo
  remote_user: "{{remote_admin}}"
  tasks:
    - name: Restart radar-clock service
      systemd:
        state: restarted
        name: radar-clock
